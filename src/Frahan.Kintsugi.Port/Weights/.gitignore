# Build outputs and very large binaries (model checkpoints fetched on demand).
kintsugi.bin
*.ckpt

# 2026-06-06: per maintainer decision, the Kintsugi training data IS bundled
# (the whole repo is GPL-3.0, so vendoring the GPL-3.0 upstream is consistent).
# puzzlefusion-plusplus/ (upstream code + configs + data-prep) and
# parity_fixtures.bin are now COMMITTED. Large model checkpoints (*.ckpt) and the
# Breaking Bad dataset stay external (fetch via the extract_breaking_bad_* scripts).
