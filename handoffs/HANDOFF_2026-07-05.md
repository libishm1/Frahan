# Frahan StonePack — Handoff 2026-07-05 (Zenodo release + session state)

Branch `edgematch/evolve`, **everything pushed** (0 unpushed) to
`github.com/libishm1/Frahan`. Full test battery green: **1051 PASS / 154 SKIP /
0 FAIL** (`FRAHAN_SKIP_NATIVE=1`). Version audit resolved: every shipped assembly,
both `.gha`, both workers, and both Yak manifests are at **0.1.0**.

Repo rule (do not forget): `D:\frahan-stonepack` (GitHub `libishm1/Frahan`) is
**canonical / most-complete**. `D:\code_ws` is a personal build/test mirror and is
stale — always work against `D:\frahan-stonepack`.

---

## 1. ZENODO RELEASE CHECKLIST (this evening) — READ FIRST

`.zenodo.json` = version `0.1.0-alpha`, title "Frahan StonePack", ORCID
0009-0004-3238-4202, GPLv3. `CITATION.cff`, `LICENSE`,
`RELEASE_NOTES_v0.1.0-alpha.md` all present.

### CRITICAL PRE-STEP — the staged .yak is STALE, rebuild it first
`install/yak_build/frahanstonepack-0.1.0-rh8_30-win.yak` was built BEFORE this
session's native fixes. Right now `install/yak_build/` still has:
- `frahan_cra_worker.exe` **v0.5.5** (should be 0.1.0)
- `frahan_recon_worker.exe` **v0.5.5** (should be 0.1.0)
- **NO `mpfr-6.dll`** (CGAL is dead without it — silent BSP fallback)

The committed `install/plugin/` IS correct (0.1.0 workers + mpfr-6.dll, fixed
this session in 3c3c33c + 93ff9e9). The `.yak` just has to be re-assembled from
it. If you ship the current `.yak`, CGAL is broken and the workers are stale.

### Rebuild the .yak (Rhino MUST be closed)
```
# 0. Close Rhino. Kill any stray workers so files aren't locked:
Get-Process frahan_*worker -EA SilentlyContinue | Stop-Process -Force

# 1. Release build (produces the 0.1.0 binaries)
dotnet build Frahan.StonePack.sln -c Release

# 2. Stage the fresh managed build into install/plugin
pwsh -File install/stage.ps1
#    stage.ps1 copies the managed .dlls + frahan_cra_worker.exe from the Release
#    build. It does NOT touch the manually-committed natives (recon_worker,
#    frahan_cgal, gmp-10, mpfr-6, osqp, coacd, geogram). Those are already correct
#    in install/plugin from this session.

# 3. VERIFY install/plugin before packaging:
Get-ChildItem install/plugin/frahan_cra_worker.exe, install/plugin/frahan_recon_worker.exe |
  ForEach-Object { "$($_.Name) v$($_.VersionInfo.FileVersion)" }   # both must read 0.1.0.0
Test-Path install/plugin/mpfr-6.dll                                 # must be True

# 4. Build the Yak package (assembles yak_build from install/plugin, runs yak build)
pwsh -File install/build_yak.ps1

# 5. VERIFY the new .yak: unzip it (a .yak is a zip) and confirm workers = 0.1.0
#    and mpfr-6.dll is present.
```

### Sanity-run the shipped worker before publishing
```
install/plugin/frahan_cra_worker.exe --selftest      # expect: IsStable=True, Optimal
```

### Publish
- Yak (Food4Rhino / Rhino Package Manager): `yak login` then `yak push <the .yak>`
  (HITL, needs McNeel creds).
- Zenodo DOI: per the 2026-06-15 note there were **3 web-UI steps pending** and the
  repo was **private**. The standard GitHub->Zenodo flow: (a) make the repo public,
  (b) enable the repo in the Zenodo GitHub integration (zenodo.org/account/settings/github),
  (c) cut a GitHub Release with tag `v0.1.0-alpha` -> Zenodo auto-archives the
  tarball and mints the DOI. Confirm the exact 3 steps against the last Zenodo
  session before publishing. Update `.zenodo.json` / `CITATION.cff` with the DOI
  once minted.

### Pre-publish gate (all green already, just re-confirm)
- `FRAHAN_SKIP_NATIVE=1` battery: 1051 PASS / 0 FAIL.
- Deploy the fresh `.gha` into a clean Rhino 8 and load one example per family
  (recon, masonry, packing, GPR, edge-match) to confirm no unresolved components.

---

## 2. What shipped this session (commit stack, all pushed)

Newest first, on `edgematch/evolve`:

| Commit | What |
|---|---|
| df19017 | **Frechet distance primitive + Edge Gap node** (R1, additive/opt-in; 8 tests) |
| 9fe24cb | **Edge-matching theory-vs-implementation study** (`wiki/research/edge_matching_theory_vs_implementation.md`) |
| 038b738 | **Auto reconstruction prefers Poisson when normals present** (full surfaces by default) |
| 27dcab7 | **Density-adaptive AlphaShape auto-alpha** (was find_optimal_alpha; ~6x fewer slivers) |
| 33623be | CGAL native ETH-stone trim test (the missing native-path test) |
| 3c3c33c | **fix: bundle mpfr-6.dll — CGAL was silently dead** (BSP fallback everywhere) |
| 93ff9e9 | **release: rebuild shipped workers 0.5.5 -> 0.1.0** (they were stale) |
| a877cf1 | examples: drop redundant 51 (dup of 40), index 33-50 + vaults |
| c097889 | careful P0-3 / epsilon pass (0 logic change; disproved 2 proposed fixes) |
| adcf951 | promote examples 49-51 (extraction plan, castle+IFC, cross-lithology) |
| 27c64dd | legibility sweep (drop "Frahan " prefix, de-acronym nicks, un-Lab, un-hide) |

Plus earlier: P0/P1 fixes (364d464 subprocess leak, 6625829 OSQP-inaccurate,
3d59f94 native free-in-finally, df3ae5c NFP reach), test version 8a8f770.

**Key native discovery + fix:** CGAL (`frahan_cgal.dll`) failed to load everywhere
(`0x8007007E`) because `mpfr-6.dll` was never bundled — only `gmp-10.dll` was. So
every CGAL call silently fell back to managed BSP, masked green by "fallback when
DLL absent" tests. Fixed by bundling the vcpkg `mpfr-6.dll`
(`C:\vcpkg\installed\x64-windows\bin\mpfr-6.dll`, byte-matched gmp confirms
provenance). Verified all natives now load: osqp, geogram, CGAL (advancing-front
+ Poisson produce meshes), quadwild, discontinuity (facets=3 on a 3-plane cloud),
coacd (CoACD 1.0.11).

---

## 3. Pending work (post-Zenodo)

### UX audit (full doc: `D:\code_ws\outputs\2026-07-05\ux_audit\UX_AUDIT.md`)
All are subcategory/exposure/icon changes = GUID-stable = **no example churn**.
- **P1** Move ~12 scan/cloud loaders from "Mesh" (42, catch-all) into "Ingest" (5).
- **P1** Split georef/align nodes out of "Mesh".
- **P1** Backend-variant clutter: Mesh Repair shows 4x (Auto/CGAL/Geogram/bare) at
  primary; demote backend-specific twins to `secondary`.
- **P2** Hide `AdaptiveBlockMatch3D` (it is `primary` but SKELETON — trim stubbed;
  its 3 sibling stubs are correctly hidden).
- **P2** Icon pass: ~90 components share ~15 icons (16x Downsample, 14x
  EdgeMatchSolve) -> siblings look identical in the ribbon.
- **P3** Fold micro-groups (Sculpt 3, Reports 3, Analysis 3, Slab 4).
- NOTE: the metadata tests assert some subcategories -> update those assertions
  alongside (as in 27c64dd).

### Edge-matching study recommendations (`wiki/research/edge_matching_theory_vs_implementation.md`)
- **R1 (primitive shipped)** — wire `FrechetDistance` into the block matchers'
  `Joint Residual` slot as an OPTIONAL accept gate (`Ordered Gap` output +
  `Max Ordered Gap` input, default off). CAN shift examples 42/48 -> do it
  deliberately WITH a re-bake, as a separate reviewed change.
- R2 geometric hashing for partial/snapped rims; R3 point-to-plane ICP fine stage;
  R4 wire edge-match output into the fabrication export; R5 MILP stock-constrained
  reuse (OrTools, the advertised-but-empty `MatcherRegistry.MilpSolver`).

### Reconstruction (done this session; context for future)
- Auto mode: Poisson when normals present, else density-adaptive AlphaShape, each
  falling through the remaining backends. AlphaShape auto-alpha =
  `(2*median NN spacing)^2` with find_optimal_alpha as the connectivity fallback.
- Alpha vs Poisson finding (Tongjiang): find_optimal_alpha = 34% slivers (worst);
  alpha=1 = 5.5%; Poisson balloons on open scans (87% off-cloud fill). Alpha shape
  interpolates (holes by design); Poisson is watertight (fills gaps). Meshes are
  baked in the `aardvark` Rhino slot if still open.

---

## 4. Environment / build / test facts

- **Rhino 8 = .NET 8 CoreCLR.** Plugin targets `net48`, `#nullable disable` per
  file, no modern-BCL overloads (net48 compat).
- **Build:** `dotnet build Frahan.StonePack.sln -c Debug|Release`. The test project
  post-build copies frahan_cgal + gmp-10 + mpfr-6 into its bin so the native CGAL
  test engages.
- **Tests:** `tests/Frahan.StonePack.Tests` — a hand-rolled harness (no xUnit),
  ~1050 tests registered in `Program.cs`. Run: `dotnet run -c Debug --no-build`.
  `FRAHAN_SKIP_NATIVE=1` skips the rhcommon_c (Rhino-runtime) tests for a clean
  headless green. `FRAHAN_TEST_FILTER=<substr>` narrows the run.
  The 154 SKIPs are Rhino-runtime-gated (rhcommon_c 0x8007045A won't init headless)
  — environmental, not failures.
- **Deploy:** `install/stage.ps1` then `install/deploy.ps1` (into Rhino) or
  `install/build_yak.ps1` (package). Rhino MUST be closed; kill stray
  `frahan_*worker` first. `deploy.ps1` + `build_yak.ps1` enumerate everything in
  `install/plugin/`, so committed natives ship automatically.
- **MCP:** Rhino MCP via router (`mcp__rhino__*`), slot "aardvark"; `run_csharp`
  is VOID (print to stdout, no return); the script host can wedge after heavy
  solves -> respawn the slot.
- **Do NOT touch** `Agent-orchestration-main/`. Commits >5 files want HITL (the
  version/legibility/release work was user-directed = authorized).

---

## 5. One-line status

v0.1.0 is code-complete and green; the ONLY thing between here and Zenodo is
**rebuilding the stale `.yak`** (Rhino closed, from the already-correct
`install/plugin/`) then the Zenodo/Yak publish steps in section 1.
